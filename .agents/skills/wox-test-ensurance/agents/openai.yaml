interface:
  display_name: "Wox Test Ensurance"
  short_description: "Ensure the Wox test suite passes"
  default_prompt: "Use $wox-test-ensurance to run Wox tests and fix any failures until the suite passes."
