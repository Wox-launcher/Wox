interface:
  display_name: "Wox Memory Debug"
  short_description: "Check Go UI memory growth after repeated searches"
  default_prompt: "Use $wox-memory-debug to run the Wox Go UI automation debug build, replay repeated searches, and diagnose retained memory growth."
