# macOS Menus Plugin

Menus searches the current app's menu bar on macOS. Type part of a menu item name while that app is focused.

## Quick Start

With Safari or another Mac app focused, open Wox and type:

```text
export
preferences
```

Select a result to invoke that menu command. This plugin is macOS only.
