# Update Plugin

Update checks for Wox releases and applies them. Triggers are `update` and `upgrade`.

![The Update plugin](/images/guide/plugin_update.jpg)

## Quick Start

```text
update
```

The result shows the current version, the latest version on your channel, and release notes. Actions can check again, download, install, or switch between the stable and beta channels.

Change the channel in **Settings -> Update -> Update channel**. See [Installation](../../installation.md).
