# Theme Manager

Theme applies, installs, removes, and restores Wox themes.

## Quick Start

```text
theme
theme ai dark graphite with teal accents
```

`theme` lists the current theme, system themes, and store themes. Press `Enter` to apply a theme.

AI generation needs a configured provider. See [Theme generation](../../ai/theme.md) and the [theme store](/store/themes).
