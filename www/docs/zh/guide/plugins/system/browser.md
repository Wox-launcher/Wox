# 浏览器插件

浏览器插件可以在浏览器集成可用时搜索并切换标签页。它是上下文插件：标签结果只针对当前正在使用的浏览器。

## 快速开始

```text
browser github
```

当受支持的浏览器处于焦点时，也可以直接输入标签标题的一部分。选中结果即可激活对应标签。

## 说明

如果没有标签结果，先为浏览器安装或启用 Wox 的浏览器集成。书签搜索是另一个插件：[浏览器书签](./browser-bookmark.md)。
