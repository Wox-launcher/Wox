# 系统命令插件

系统命令可以执行锁屏、休眠、音量和设置等系统动作。它监听全局输入，所以可以直接输入 `lock`、`sleep` 或 `shutdown`。

![系统命令插件](/images/guide/plugin_sys.jpg)

## 快速开始

```text
lock
sleep
empty recycle
set volume 50
confetti
```

可用命令取决于平台。关机这类破坏性动作会先确认。`confetti` 会显示全屏彩纸效果。
