# 更新插件

更新插件检查并安装 Wox 新版本。触发关键字是 `update` 和 `upgrade`。

## 快速开始

```text
update
```

结果会显示当前版本、当前通道上的最新版本和更新说明。动作可以再次检查、下载、安装，或在稳定版和测试版通道之间切换。

在 **设置 -> 更新 -> 更新通道** 中修改通道。见 [安装](../../installation.md)。
