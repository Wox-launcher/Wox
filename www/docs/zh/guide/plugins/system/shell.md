# Shell 插件

Shell 可以从 Wox 运行命令，并保留命令历史。用 `>` 明确进入 shell 查询。部分命令也可以被全局识别。

![Shell 插件](/images/guide/plugin_shell.jpg)

## 快速开始

```text
> git status
> npm test
```

正在运行的命令会在历史里显示指示器。可以在插件设置里指定默认工作目录，或为单条命令选择目录（家目录、上次使用的目录或自定义目录）。

操作面板可以把输出保存到笔记、重新运行上一条命令，或在系统终端中打开当前命令。
