# Calculator 插件

Calculator 用于直接计算数学表达式，并保留最近记录。

## 能做什么

- 自动识别含操作符的表达式
- 支持 `+ - * / ^` 与括号
- 默认显示千分位
- 保存最近计算结果

## 快速开始

```
100 + 200
2 ^ 10
(500 - 100) / (10 + 10)
```

## 历史记录

输入 `calculator` 查看历史，可复制结果或重新计算。

## 注意事项

- 必须包含操作符才会触发计算（如 `123 + 1`）。
- 复制结果支持原始值和带千分位的值。
